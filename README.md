This repository contains a clone of an existing website, created as a learning exercise.

The primary objective is to analyze the website’s layout system and understand
how CSS Grid is used to structure and build the UI components.
